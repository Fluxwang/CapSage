# 09 — 侧边栏「总结和仿写」子页 + popup 按钮改为唤出侧边栏

**What to build:** 侧边栏第二个子页落地：把这条视频的字幕提炼成编号的"视频要点"，以及按选定的口播时长/类型生成"仿写脚本"。popup 里的"总结"和"仿写"两个按钮不再试图在 400px 宽的面板里展开内容，而是直接唤出侧边栏并跳到这个子页。

**Blocked by:** 07（侧边栏骨架）

**Status:** ready-for-agent

底层的总结/仿写接口调用在 02 已经迁移过来了，这一票是把它接到新的呈现面上，并补上设计稿里新增的生成选项。

- [ ] 「总结和仿写」子页：编号的视频要点列表 + "重新生成"
- [ ] 仿写脚本生成器，带口播时长/类型选择（设计稿示例："口播 · 30 秒"）
- [ ] popup 的"总结"/"仿写"按钮唤出侧边栏对应子页，不在 popup 内展开内容
- [ ] 生成过程中有明确的进行中状态，失败有可读的错误提示而不是静默
- [ ] 未配置 Base URL / API Key / 聊天模型时，给出指向设置页的明确提示
