# EchoSage

一个 Chrome 扩展：捕获标签页音频/视频字幕来源，转写、翻译成中文、以字幕形式呈现。覆盖两个场景——任意网站的实时直播语音，和 TikTok 视频的字幕获取——共用转写、翻译、字幕呈现这三层基础设施。原为两个互不相关的独立扩展（EchoCap 负责直播实时字幕，TikTok Caption Studio 负责视频字幕/摘要/仿写），本次合并为一个产品。

## Language

### 场景

**直播实时 (Live Scene)**:
面向任意标签页的实时语音场景——捕获当前标签页音频，持续转写、逐句/整段翻译、悬浮渲染。没有天然的结束点，靠用户手动停止或长静音自动断连结束。原 EchoCap 的全部能力。
_Avoid_: 直播模式、EchoCap

**视频字幕 (Video Scene)**:
面向 TikTok 视频详情页/信息流的字幕场景——字幕文本优先取原生 WebVTT 轨，只有该视频没有原生字幕轨时才触发转写。一段视频的转写有天然结束点（见"单轮转写"）。原 TikTok Caption Studio 的全部能力，现在字幕来源新增了"转写"这一分支。
_Avoid_: 视频模式、TikTok Caption Studio

### 音频与转写

**捕获 (Capture)**:
从某个标签页取得音频流的会话，从 `tabCapture` 拿到 streamId 开始，到 track 被 stop 结束。两个场景共用同一条捕获实现。
_Avoid_: 录制、录音（本项目不落盘，没有"录"这回事）

**回放链路 (Playback Path)**:
把捕获到的音频原样送回扬声器的那条音频图分支。`tabCapture` 会静音原标签页，不接回去用户就听不到声音。
_Avoid_: 监听、监控

**转写链路 (ASR Path)**:
把捕获到的音频转成 16kHz 单声道 PCM16、切片后送往 AssemblyAI 的那条音频图分支，与回放链路各自跑在独立的 AudioContext 上。
_Avoid_: 识别链路

**音频块 (Chunk)**:
一次通过 WebSocket 发给 AssemblyAI 的 PCM16 载荷，受协议约束需承载 50ms–1000ms 音频。
_Avoid_: 帧、包、buffer

**转写来源优先级 (Transcript Source Priority)**:
视频字幕场景专属的概念：字幕文本优先取 TikTok 原生 WebVTT 轨；只有该视频没有原生字幕轨时，才触发转写（AssemblyAI 实时流）。直播实时场景没有"原生字幕"这回事，永远走转写。
_Avoid_: 兜底转写（旧叫法，暗示"众多转写引擎里退而求其次的一个"——现在转写只有 AssemblyAI 一种，不是被挑剩下的选项）

**话轮 (Turn)**:
AssemblyAI 流式转写切分出的一段连续讲话，靠静音检测断句，大致对应一个自然句。两个场景通用的转写产出单位；同一个话轮先后给出「未完成」和「已完成」两种状态，已产出文本永不改写（只增不改）。
_Avoid_: 句子、utterance、段落

**单轮转写 (One Playthrough)**:
视频字幕场景专属：一段视频的转写会话在播放到头（`currentTime` 从接近 `duration` 跳回接近 0）时自动结束，不管视频是否继续循环播放。直播实时场景没有这个概念——它没有天然终点。
_Avoid_: 循环检测、固定录制时长（不是时长上限，是"播完一遍"这个事件）

**源语言 (Source Language)**:
用户为当前会话选择的转写语言，驱动 AssemblyAI 的 `language_codes` 和（翻译引擎为 Chrome 内置时）Translator 的 `sourceLanguage`。两个场景都可选，不写死西班牙语。目标语言固定中文，不可选。
_Avoid_: 直播语言、字幕语言

### 翻译

**转写 (Transcript)**:
AssemblyAI 返回的源语言文本。
**译文 (Translation)**:
由转写产出的中文文本。
**字幕 (Subtitle)**:
渲染层的呈现内容，由原文轨+译文轨组成。字幕是呈现层概念，转写和译文是数据层概念——三者不可混用。
**原文轨 (Source Track)**:
字幕里显示源语言转写的那一行，职责是**活跃度指示**（提示用户系统还活着），不是信息传递，视觉上刻意弱化。
**译文轨 (Translation Track)**:
字幕里显示中文译文的那一行，是主视觉。

**翻译引擎 (Translation Engine)**:
把转写变成译文所用的方式，全局设置里二选一：Chrome 内置 nano 或 AI 模型。两者的翻译时机不同（见下），是"同一份转写数据、两种译法"的分叉点，两个场景通用。
_Avoid_: 翻译方式、翻译模型（后者和"聊天模型"容易混）

**逐句翻译 (Per-Turn Translation)**:
Chrome 内置 nano 的行为：每个话轮定稿就立刻在本地翻译，不等会话结束。是默认引擎。
_Avoid_: 实时翻译（和"实时转写"放一起容易指代不清）

**整段批量翻译 (Batch Translation)**:
AI 模型的行为：等这段转写完全结束（会话停止 / 视频播完一轮）后，把累积的全部文本一次性发给已配置的 AI 接口换回整段译文；会话进行中译文轨留空或标记"翻译中"。AI 模型在本项目里不支持逐句调用。
_Avoid_: 逐句调用、AI 逐句翻译

### 呈现

**悬浮层 (Overlay)**:
盖在页面视频画面上的双轨字幕浮层。直播实时场景必定使用；视频字幕场景可通过"悬浮窗"开关选用。同一个渲染组件接受两种数据喂法：直播的实时流式话轮，和视频字幕已攒好的、带时间戳的静态字幕数组 + 当前播放进度。
_Avoid_: 字幕层（和"字幕"这个数据层概念混）

**侧边栏 (Sidepanel)**:
视频字幕场景专属，从页面右侧推开显示的完整工作区，含"字幕和翻译"与"总结和仿写"两个子页，从 popup 的"侧边栏"入口唤出。是字幕列表/总结/仿写这些内容的主要呈现处；popup 只放紧凑预览。
_Avoid_: 侧边面板、抽屉（旧版 TikTok Caption Studio 里较简陋的字幕列表叫"字幕抽屉"，现在这个更完整的形态改叫侧边栏）
