{
  "name": "echosage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "EchoSage Chrome 扩展",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node --test core/*.test.js",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
